package bgu.spl.mics;

/*
class GPUTest {
    GPU g1;
    GPU g2;
    GPU g3;
    Data d;
    Student s;
    Model m;
    @BeforeEach
    void setUp() {
        g1 = new GPU("RTX3090");
        g2 = new GPU("RTX2080");
        g3 = new GPU("GTX1080");
        d = new Data("Text",3000);
        s = new Student("Ben","Computer-Science","PhD");
        m = new Model("hi",d,s);
    }

    @Test
    void getType() {
        setUp();
        assertEquals(g1.getType(), RTX3090);
        assertEquals(g2.getType(), RTX2080);
        assertEquals(g3.getType(), GTX1080);
    }

    @Test
    void getModel() {
        setUp();
        assertNull(g1.getModel());
        assertNull(g2.getModel());
        assertNull(g3.getModel());
        setUp();
        g1.divideToDataBatches(m);
        g2.divideToDataBatches(m);
        g3.divideToDataBatches(m);
        assertEquals(g1.getModel(),m);
        assertEquals(g2.getModel(),m);
        assertEquals(g3.getModel(),m);
    }

    @Test
    void divideToDataBatches() {
        setUp();
        g1.divideToDataBatches(m);
        g2.divideToDataBatches(m);
        g3.divideToDataBatches(m);
        assertEquals(g1.getDataBatches().size(),3);
        assertEquals(g2.getDataBatches().size(),3);
        assertEquals(g3.getDataBatches().size(),3);
    }
}
 */
